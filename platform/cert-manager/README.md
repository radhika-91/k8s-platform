# cert-manager

Wraps [cert-manager](https://cert-manager.io/) for automated TLS. Use with ingress for HTTP-01 or DNS-01 issuers in production.

Toggle: `features.certManager.enabled` in `clusters/<name>/cluster.yaml`.
