# Policy

Wraps [Kyverno](https://kyverno.io/) for Kubernetes policy enforcement.

Toggle: `features.policy.enabled` in `clusters/<name>/k8s-features.yaml`.

Add ClusterPolicy manifests in a follow-up chart or directory when ready for production baselines.
