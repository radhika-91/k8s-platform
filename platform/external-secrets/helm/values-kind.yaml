external-secrets:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

# Kind: configure a ClusterSecretStore after install (Vault, AWS SM, etc.).
# Do not commit credentials — use ExternalSecret resources in a separate repo or SOPS.
