# External Secrets

Wraps [External Secrets Operator](https://external-secrets.io/) for syncing secrets from external providers.

Toggle: `features.externalSecrets.enabled` in `clusters/<name>/cluster.yaml`.
