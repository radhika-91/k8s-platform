ingress-nginx:
  controller:
    hostPort:
      enabled: true
      ports:
        http: 80
        https: 443
    service:
      type: ClusterIP
    watchIngressWithoutClass: true
    config:
      use-forwarded-headers: "true"
