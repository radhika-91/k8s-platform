# Ingress

Wraps [ingress-nginx](https://kubernetes.github.io/ingress-nginx/) with Kind-friendly hostPort settings aligned to `bootstrap/kind/kind-config.yaml` port mappings.

Toggle: `features.ingress.enabled` in `clusters/<name>/cluster.yaml`.
